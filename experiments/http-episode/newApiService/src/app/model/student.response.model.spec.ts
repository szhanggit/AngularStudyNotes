import { StudentResponseModel } from './student.response.model';

describe('StudentResponseModel', () => {
  it('should create an instance', () => {
    expect(new StudentResponseModel()).toBeTruthy();
  });
});
