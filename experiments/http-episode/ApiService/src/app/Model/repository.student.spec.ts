import { Repository.Student } from './repository.student';

describe('Repository.Student', () => {
  it('should create an instance', () => {
    expect(new Repository.Student()).toBeTruthy();
  });
});
