import { KeyValidateResponse } from './key-validate-response.model';

describe('KeyValidateResponse', () => {
  it('should create an instance', () => {
    expect(new KeyValidateResponse()).toBeTruthy();
  });
});
