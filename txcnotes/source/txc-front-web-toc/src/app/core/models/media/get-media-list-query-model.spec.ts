import { GetMediaListQueryModel } from './get-media-list-query-model';

describe('GetMediaListQueryModel', () => {
  it('should create an instance', () => {
    expect(new GetMediaListQueryModel()).toBeTruthy();
  });
});
