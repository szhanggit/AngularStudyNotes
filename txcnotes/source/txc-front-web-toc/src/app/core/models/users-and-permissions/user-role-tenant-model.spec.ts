import { UserRoleTenantModel } from './user-role-tenant-model';

describe('UserRoleTenantModel', () => {
  it('should create an instance', () => {
    expect(new UserRoleTenantModel()).toBeTruthy();
  });
});
