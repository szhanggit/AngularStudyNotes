FROM node:18-alpine3.16 as build-step
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install --force
COPY . .
RUN npm run build-uat --prod

FROM nginx:1.23.2-alpine as prod-stage
COPY --from=build-step /app/dist/web-toc /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]