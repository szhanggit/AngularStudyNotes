export interface ExpirationPolicy {
    expirationPolicyId: number,
    inUse: number,
    expirationPolicyName: string,
    typeId: number,
    typeName: string,
    isEndOfDay: number,
    validPeriod: number
}
