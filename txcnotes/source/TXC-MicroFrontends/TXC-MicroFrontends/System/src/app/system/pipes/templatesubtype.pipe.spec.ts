import { TemplatesubtypePipe } from './templatesubtype.pipe';

describe('TemplatesubtypePipe', () => {
  it('create an instance', () => {
    const pipe = new TemplatesubtypePipe();
    expect(pipe).toBeTruthy();
  });
});
