https://code.visualstudio.com/      (Install Visual Studio Code)
node -v                 (Check if nodejs already has been installed)
npm -v                        (Check if npm already has been installed)
https://nodejs.org/en/download/     (Go to install nodejs)
Once nodejs is installed, npm shall be installed too.
tsc -v
npm install –g typescript     (Install typescript)
npm install -g @angular/cli   (Install Angular CLI)
ng v                    (Check if cli has been installed)
npm install --force           (Download packages in a specific project)