import { GetUserListQueryModel } from './get-user-list-query-model';

describe('GetUserListQueryModel', () => {
  it('should create an instance', () => {
    expect(new GetUserListQueryModel()).toBeTruthy();
  });
});
