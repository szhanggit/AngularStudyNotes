import { GetImageListResponseModel } from './get-image-list-response-model';

describe('GetImageListResponseModel', () => {
  it('should create an instance', () => {
    expect(new GetImageListResponseModel()).toBeTruthy();
  });
});
