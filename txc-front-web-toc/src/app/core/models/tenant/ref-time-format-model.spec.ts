import { RefTimeFormatModel } from './ref-time-format-model';

describe('RefTimeFormatModel', () => {
  it('should create an instance', () => {
    expect(new RefTimeFormatModel()).toBeTruthy();
  });
});
