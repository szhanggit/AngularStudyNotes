import { TenantDetailModel } from './tenant-detail-model';

describe('TenantDetailModel', () => {
  it('should create an instance', () => {
    expect(new TenantDetailModel()).toBeTruthy();
  });
});
