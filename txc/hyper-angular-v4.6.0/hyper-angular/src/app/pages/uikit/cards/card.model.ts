interface StrechedLinkCard {
    image: string;
    color: string;
}

interface PortletCards {
    id: number;
    bgColor?: string;
    textColor?: string;
}

export { StrechedLinkCard, PortletCards };